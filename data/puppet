[puppet-lint]
apt = puppet-lint
match = *.pp
command = puppet-lint {files}

[puppet]
apt = puppet
match = *.pp
command = puppet parser validate {files}

# TODO: erb files?
# erb -x -T '-' {files} | ruby -c

# vim:ft=dosini
