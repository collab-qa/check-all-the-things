[make]
apt = make
files = GNUmakefile Makefile *.make *.mk
types = text/x-makefile
command = make --question --makefile {file}
flags = dangerous

[make-phony]
files = debian/rules
types = text/x-makefile
comment = common misspelling of .PHONY
command = grep -Ew '[.]PHONEY?' {files}

# vim:ft=dosini
