[jpeginfo]
flags = images fixme fixme-silent
debian = jpeginfo
files = *.jpg *.jpeg
types = image/jpeg
command = jpeginfo --check --quiet {files} | grep -v -F '[OK]'

# TODO: identify -verbose {file} | grep -i copyright && echo {file}
# TODO: jpegoptim
