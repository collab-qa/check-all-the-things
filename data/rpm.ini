[rpmlint]
comment = Dangerous because parsing RPM spec files executes arbitrary code
flags = package dangerous
debian = rpmlint
files = *.rpm *.spec
types = application/x-rpm
command = rpmlint {cwd}

# TODO: https://github.com/dcantrell/rpminspect
