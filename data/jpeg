[jpeginfo]
flags = images fixme fixme-silent
apt = jpeginfo
files = *.jpg *.jpeg
types = image/jpeg
command = jpeginfo --check --quiet {files} | fgrep -v '[OK]'

# TODO: identify -verbose {file} | grep -i copyright && echo {file}
# TODO: jpegoptim

# vim:ft=dosini
